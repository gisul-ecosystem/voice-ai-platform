# STT service has no external dependencies to configure yet.
# Placeholder for future config (model variant, device, batch size).
# Bind port is set by uvicorn --port (must be 5552). This value is documentation only.
SERVICE_PORT=5552
