# Server-only FastAPI origin. Never expose this as NEXT_PUBLIC_*.
BACKEND_API_URL=http://127.0.0.1:5554
BACKEND_SERVICE_TOKEN=

# Browser feature flag. Keep false for audio-only testing; set true at deploy time.
NEXT_PUBLIC_INTERVIEW_CAMERA_ENABLED=false
